name: token
args: message: the message to ask the user
args: field: the name of the field (i.e. "key", "token")
args: env: the name of the environment variable to set
args: sensitive: (optional, default true) if true, the asterisks will be shown as the user types, rather than plain text

#!${GPTSCRIPT_TOOL_DIR}/bin/gptscript-go-tool "${GPTSCRIPT_INPUT}"
